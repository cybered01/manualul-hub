---
import Layout from '../layouts/Layout.astro';
import WhatsAppButton from '../components/WhatsAppButton.astro';

const title = "Calculator Diluție Vermorel - Manualul.ro";
const description = "Calculează doza exactă de soluție pentru pompa ta (vermorel). Evită arderea plantelor prin diluție corectă.";
---

<Layout title={title} description={description}>
    <main class="max-w-4xl mx-auto px-4 pb-20">
        <header class="py-12 text-center">
            <div class="inline-block p-3 bg-purple-100 rounded-2xl mb-4">
                <span class="text-4xl">🧪</span>
            </div>
            <h1 class="text-4xl md:text-6xl font-black text-gray-900 mb-4 tracking-tight">
                Calculator <span class="text-purple-600">Diluție</span>
            </h1>
            <p class="text-lg text-gray-600 max-w-2xl mx-auto font-medium">
                Calculează rapid câtă soluție (pesticid, fungicid, îngrășământ) trebuie să pui în pompa ta în funcție de recomandările de pe etichetă.
            </p>
        </header>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div class="lg:col-span-2 space-y-6">
                <div class="bg-white rounded-[40px] shadow-xl border border-purple-50 overflow-hidden">
                    <!-- Card 1: Referinta Eticheta -->
                    <div class="p-8 bg-purple-600 text-white">
                        <h2 class="text-xl font-bold mb-6 flex items-center gap-2">
                            <span>📋</span> Referință Etichetă
                        </h2>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div class="space-y-2">
                                <label for="doza_rec" class="block text-xs font-black uppercase opacity-80 tracking-wider">Doză recomandată</label>
                                <div class="relative">
                                    <input type="number" id="doza_rec" value="200" min="0" step="1" class="w-full bg-white/10 border border-white/20 rounded-2xl p-4 text-white font-bold text-2xl outline-none focus:bg-white/20 transition-all">
                                    <span class="absolute right-4 top-1/2 -translate-y-1/2 font-bold opacity-60 text-sm">ml/g</span>
                                </div>
                            </div>
                            <div class="space-y-2">
                                <label for="volum_ref" class="block text-xs font-black uppercase opacity-80 tracking-wider">La volumul de apă</label>
                                <div class="relative">
                                    <input type="number" id="volum_ref" value="100" min="1" step="1" class="w-full bg-white/10 border border-white/20 rounded-2xl p-4 text-white font-bold text-2xl outline-none focus:bg-white/20 transition-all">
                                    <span class="absolute right-4 top-1/2 -translate-y-1/2 font-bold opacity-60 text-sm">Litri</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Card 2: Echipamentul Tau -->
                    <div class="p-8 md:p-10 space-y-8">
                        <div class="space-y-4">
                            <label for="volum_pompa" class="text-[10px] font-black text-purple-600 uppercase tracking-[0.2em]">Echipamentul Tău (Capacitate Pompă)</label>
                            <div class="relative">
                                <input type="number" id="volum_pompa" value="12" min="0" step="0.5" class="w-full bg-gray-50 border border-gray-200 rounded-2xl p-4 text-gray-900 font-bold text-2xl outline-none focus:border-purple-500 transition-all">
                                <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 font-bold">Litri</span>
                            </div>

                            <div class="flex flex-wrap gap-2">
                                <button type="button" id="btn-10l" class="px-4 py-2 rounded-xl bg-purple-50 text-purple-700 font-bold text-sm hover:bg-purple-100 transition-all border border-purple-100">10L</button>
                                <button type="button" id="btn-12l" class="px-4 py-2 rounded-xl bg-purple-50 text-purple-700 font-bold text-sm hover:bg-purple-100 transition-all border border-purple-100">12L</button>
                                <button type="button" id="btn-15l" class="px-4 py-2 rounded-xl bg-purple-50 text-purple-700 font-bold text-sm hover:bg-purple-100 transition-all border border-purple-100">15L</button>
                                <button type="button" id="btn-400l" class="px-4 py-2 rounded-xl bg-purple-50 text-purple-700 font-bold text-sm hover:bg-purple-100 transition-all border border-purple-100">400L</button>
                            </div>
                        </div>

                        <div class="mt-12 p-8 rounded-[40px] bg-purple-50 border-2 border-dashed border-purple-200 text-center md:text-left">
                            <div class="flex flex-col md:flex-row justify-between items-center gap-8">
                                <div>
                                    <p class="text-[10px] font-black uppercase text-purple-400 tracking-[0.2em] mb-1">Rezultat Calcul</p>
                                    <p class="text-4xl md:text-5xl font-black text-purple-700" id="rezultatDisplay">0 ml</p>
                                    <p class="mt-2 text-sm font-bold text-purple-900">Adaugă soluția în pompa ta.</p>
                                </div>
                                <WhatsAppButton idDesktop="whatsappBtn" idMobile="whatsappBtnFloating" />
                            </div>
                        </div>

                        <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded-r-2xl">
                            <div class="flex">
                                <div class="flex-shrink-0 text-xl">⚠️</div>
                                <div class="ml-3">
                                    <p class="text-sm text-yellow-700 font-bold">
                                        Poartă mască și mănuși! Citește fișa de securitate a produsului.
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="space-y-6">
                <div class="bg-gray-900 text-white p-8 rounded-[40px] shadow-xl">
                    <h3 class="text-xl font-bold mb-6 flex items-center gap-2">
                        <span class="text-purple-400">💡</span> Sfaturi Agri
                    </h3>
                    <ul class="space-y-4 text-sm text-gray-300 font-medium">
                        <li class="flex gap-3">
                            <span class="text-purple-400 font-bold">1.</span>
                            Pune întâi jumătate din cantitatea de apă.
                        </li>
                        <li class="flex gap-3">
                            <span class="text-purple-400 font-bold">2.</span>
                            Adaugă doza de substanță calculată.
                        </li>
                        <li class="flex gap-3">
                            <span class="text-purple-400 font-bold">3.</span>
                            Completează cu restul de apă și omogenizează.
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </main>

    <script is:inline>
        function updateCalculator() {
            const dozaRec = parseFloat(document.getElementById('doza_rec').value) || 0;
            const volumRef = parseFloat(document.getElementById('volum_ref').value) || 1;
            const volumPompa = parseFloat(document.getElementById('volum_pompa').value) || 0;

            const rezultat = (dozaRec / volumRef) * volumPompa;

            const display = document.getElementById('rezultatDisplay');
            display.innerText = rezultat.toFixed(1).replace('.0', '') + " ml/g";

            // WhatsApp Link
            const btnDesktop = document.getElementById('whatsappBtn');
            const btnMobile = document.getElementById('whatsappBtnFloating');

            const mesaj = `Am calculat doza pentru vermorel: pentru o pompă de ${volumPompa}L, am nevoie de ${rezultat.toFixed(1).replace('.0', '')} ml/g de soluție. Calculează și tu: ${window.location.href}`;
            const finalLink = `https://api.whatsapp.com/send?text=${encodeURIComponent(mesaj)}`;

            if (btnDesktop) btnDesktop.href = finalLink;
            if (btnMobile) btnMobile.href = finalLink;
        }

        // Event Listeners
        document.querySelectorAll('input').forEach(input => {
            input.addEventListener('input', updateCalculator);
        });

        document.getElementById('btn-10l').addEventListener('click', () => {
            document.getElementById('volum_pompa').value = 10;
            updateCalculator();
        });
        document.getElementById('btn-12l').addEventListener('click', () => {
            document.getElementById('volum_pompa').value = 12;
            updateCalculator();
        });
        document.getElementById('btn-15l').addEventListener('click', () => {
            document.getElementById('volum_pompa').value = 15;
            updateCalculator();
        });
        document.getElementById('btn-400l').addEventListener('click', () => {
            document.getElementById('volum_pompa').value = 400;
            updateCalculator();
        });

        // Initial run
        updateCalculator();
    </script>
</Layout>
