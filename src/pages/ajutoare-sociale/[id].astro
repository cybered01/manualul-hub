---
export const prerender = true;
import Layout from '../../layouts/Layout.astro';
import { readFileSync } from 'fs';
import { parse } from 'csv-parse/sync';

export async function getStaticPaths() {
    const fileContent = readFileSync('./src/data/ajutoare.csv', 'utf-8');
    const records = parse(fileContent, { columns: true, skip_empty_lines: true, trim: true });
    return records.map((row) => ({ params: { id: row.id }, props: { info: row } }));
}

const { info } = Astro.props;
const renderList = (text) => text ? text.split('<br>').filter(Boolean) : [];
---

<Layout 
    title={`Ghid Complet: ${info.titlu}`}
    description={`Procedura oficială pentru ${info.titlu}: acte necesare, instituții responsabile și pași de urmat.`}
>
    <main class="max-w-4xl mx-auto px-4 pb-16">
        <nav class="py-6">
            <a href="/ajutoare-sociale" class="text-sm font-bold text-green-700 hover:text-green-900 flex items-center gap-2">
                ← Înapoi la listă
            </a>
        </nav>

        <header class="mb-12 border-b border-gray-100 pb-10">
            <div class="flex items-center gap-3 mb-4">
                <span class="px-3 py-1 bg-green-100 text-green-700 rounded-full text-[10px] font-black uppercase tracking-widest border border-green-200">
                    {info.status}
                </span>
                <span class="text-gray-400 text-xs font-bold uppercase">{info.categorie}</span>
            </div>
            <h1 class="text-4xl md:text-5xl font-black text-gray-900 mb-4">{info.titlu}</h1>
            <p class="text-xl text-gray-500 leading-relaxed">{info.descriere}</p>
        </header>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
            <div class="lg:col-span-2 space-y-12">
                
                <section>
                    <h2 class="text-2xl font-black text-gray-900 mb-6 flex items-center gap-3">
                        <span class="flex items-center justify-center w-8 h-8 rounded-lg bg-green-600 text-white text-sm">1</span>
                        Cine are dreptul? (Criterii)
                    </h2>
                    <div class="grid grid-cols-1 gap-4">
                        {renderList(info.criterii_eligibilitate).map((criteriu) => (
                            <div class="p-4 bg-white border border-gray-100 rounded-2xl shadow-sm flex items-start gap-3">
                                <span class="text-green-600 font-bold">✓</span>
                                <span class="text-gray-700 font-medium">{criteriu}</span>
                            </div>
                        ))}
                    </div>
                </section>

                <section>
                    <h2 class="text-2xl font-black text-gray-900 mb-6 flex items-center gap-3">
                        <span class="flex items-center justify-center w-8 h-8 rounded-lg bg-green-600 text-white text-sm">2</span>
                        Ce trebuie să faci? (Procedura)
                    </h2>
                    <div class="space-y-6">
                        {renderList(info.pasi_procedura).map((pas, index) => (
                            <div class="flex gap-6 relative">
                                <div class="flex-shrink-0 w-10 h-10 rounded-full bg-gray-900 text-white flex items-center justify-center font-bold z-10">
                                    {index + 1}
                                </div>
                                <div class="pt-2">
                                    <p class="text-gray-700 leading-relaxed font-medium">{pas}</p>
                                </div>
                            </div>
                        ))}
                    </div>
                </section>
            </div>

            <div class="space-y-8">
                <div class="bg-gray-900 text-white p-6 rounded-3xl shadow-xl">
                    <p class="text-[10px] font-black uppercase tracking-widest opacity-60 mb-2">Instituția Responsabilă</p>
                    <h3 class="text-lg font-bold mb-4">{info.institutie}</h3>
                    <div class="text-xs bg-white/10 p-4 rounded-xl border border-white/10 italic text-blue-100">
                        Sfat: Mergi la sediul local (Primărie sau Poștă) pentru a solicita formularele tipizate.
                    </div>
                </div>

                <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm">
                    <h3 class="font-bold text-gray-900 mb-4 flex items-center gap-2">
                        <span class="text-lg">📂</span> Acte necesare
                    </h3>
                    <ul class="space-y-4">
                        {renderList(info.acte_necesare).map((act) => (
                            <li class="flex items-start gap-2 text-sm text-gray-600 border-b border-gray-50 pb-2 last:border-0">
                                <span class="text-green-600 font-bold">•</span>
                                {act}
                            </li>
                        ))}
                    </ul>
                </div>

                <div class="bg-green-50 p-6 rounded-3xl border border-green-100">
                    <p class="text-[10px] font-black uppercase tracking-widest text-green-700 mb-1">Cuantum Ajutor</p>
                    <p class="text-3xl font-black text-green-900">{info.valoare_ajutor}</p>
                </div>
            </div>
        </div>
    </main>
</Layout>