---
export const prerender = true;
import Layout from '../../layouts/Layout.astro';
import records from '../../data/finantari.json';

export async function getStaticPaths() {
    return records.map((row) => ({ params: { id: row.id }, props: { info: row } }));
}

const { info } = Astro.props;
const cofinantareProcent = parseFloat(info.cofinantare) / 100;
const valoareMaxGrant = parseFloat(info.valoare_maxima.replace(/[^\d]/g, ''));
const isOpen = info.status.toLowerCase() === 'deschis';
const statusColor = isOpen ? 'text-green-600 bg-green-50 border-green-100' : 'text-amber-600 bg-amber-50 border-amber-100';

// Funcție simplă pentru a randa HTML-ul din CSV (pentru listele cu <br>)
const renderList = (text) => text.split('<br>').filter(Boolean);
---

<Layout 
    title={`Ghid Complet: ${info.titlu} 2026`} 
    description={`Manual de accesare pentru ${info.titlu}. Vezi lista de cheltuieli eligibile, actele necesare și calculează suma exactă.`}
>
    <main class="max-w-4xl mx-auto px-4 pb-12">
        <nav class="py-4">
            <a href="/finantari" class="text-sm text-blue-700 hover:underline flex items-center gap-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                </svg>
                Înapoi la lista de programe
            </a>
        </nav>

        <header class="text-center mb-10">
            <span class={`inline-block px-4 py-1 rounded-full text-[10px] font-bold uppercase tracking-widest border mb-4 ${statusColor}`}>
                {info.status}
            </span>
            <h1 class="text-3xl md:text-5xl font-black text-gray-900 tracking-tight mb-4">
                {info.titlu}
            </h1>
            <p class="text-xl text-gray-600 max-w-2xl mx-auto">{info.descriere}</p>
        </header>
        
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-16">
            <div class="lg:col-span-2">
                <div class="bg-white rounded-3xl shadow-xl border border-gray-100 overflow-hidden sticky top-8">
                    <div class="p-8 bg-blue-600 text-white">
                        <h2 class="text-xl font-bold flex items-center gap-2 mb-4">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z" /></svg>
                            Simulator Buget
                        </h2>
                        <label for="investitieInput" class="block text-xs font-bold text-blue-100 uppercase tracking-widest mb-2">
                            Investiția Totală Estimată (EUR):
                        </label>
                        <div class="relative">
                            <input 
                                type="number" 
                                id="investitieInput" 
                                value={valoareMaxGrant} 
                                class="w-full text-4xl font-black bg-white/10 border-2 border-white/20 rounded-2xl px-6 py-4 text-white focus:bg-white/20 focus:outline-none transition-all shadow-inner placeholder-blue-200"
                            />
                            <div class="absolute right-6 top-1/2 -translate-y-1/2 text-blue-200 font-bold text-xl pointer-events-none">EUR</div>
                        </div>
                    </div>

                    <div class="p-8 bg-white">
                        <div class="flex flex-col md:flex-row justify-between items-center md:items-end mb-8 gap-4 border-b border-gray-100 pb-8">
                            <div>
                                <p class="text-xs text-gray-400 uppercase font-black tracking-widest mb-1">Grant Nerambursabil</p>
                                <div class="flex items-baseline">
                                    <span id="grantDisplay" class="text-5xl font-black text-blue-600 tabular-nums">0</span>
                                    <span class="text-xl font-bold text-gray-400 ml-2">EUR</span>
                                </div>
                            </div>
                            <div class="text-right">
                                <p class="text-xs text-gray-400 uppercase font-black tracking-widest mb-1">Contribuția Ta</p>
                                <div class="flex items-baseline justify-end">
                                    <span id="cofinantareDisplay" class="text-3xl font-black text-gray-800 tabular-nums">0</span>
                                    <span class="text-lg font-bold text-gray-400 ml-2">EUR</span>
                                </div>
                                <p class="text-xs text-blue-600 mt-1 font-bold">Cofinanțare: {info.cofinantare}</p>
                            </div>
                        </div>
                        
                        <div class="bg-blue-50 p-4 rounded-xl border border-blue-100 flex gap-3">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-blue-600 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                            <p class="text-sm text-blue-800">
                                <strong>Important:</strong> Acesta este un calcul estimativ. Suma maximă pe care o poți cere este plafonată la <strong>{info.valoare_maxima}</strong> conform ghidului.
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="space-y-6">
                <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm">
                    <h3 class="font-bold text-gray-900 mb-4 flex items-center gap-2">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs">✓</span>
                        Cine poate aplica?
                    </h3>
                    <p class="text-sm text-gray-600 leading-relaxed mb-4">
                        {info.eligibilitate_extinsa}
                    </p>
                </div>

                <div class="bg-white p-6 rounded-3xl border border-gray-100 shadow-sm">
                    <h3 class="font-bold text-gray-900 mb-4 flex items-center gap-2">
                        <span class="w-6 h-6 bg-amber-100 text-amber-600 rounded-full flex items-center justify-center text-xs">📂</span>
                        Acte Necesare
                    </h3>
                    <ul class="space-y-3">
                        {renderList(info.acte_necesare).map((act) => (
                            <li class="flex items-start gap-2 text-sm text-gray-600">
                                <span class="text-amber-500 mt-0.5">•</span>
                                <span>{act}</span>
                            </li>
                        ))}
                    </ul>
                </div>
            </div>
        </div>

        <section class="max-w-none bg-white rounded-3xl p-8 md:p-12 border border-gray-100 shadow-sm">
            <h2 class="text-3xl font-black text-gray-900 mb-8">Ce poți cumpăra cu banii?</h2>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
                <div>
                    <h3 class="text-lg font-bold text-blue-700 mb-4 uppercase tracking-widest border-b border-gray-100 pb-2">Cheltuieli Eligibile</h3>
                    <ul class="space-y-4">
                        {renderList(info.cheltuieli_eligibile).map((item) => (
                            <li class="flex items-start gap-3">
                                <div class="w-6 h-6 rounded-full bg-blue-50 text-blue-600 flex items-center justify-center flex-shrink-0 font-bold text-sm">✓</div>
                                <span class="text-gray-700 font-medium">{item}</span>
                            </li>
                        ))}
                    </ul>
                </div>
                
                <div class="bg-gray-50 rounded-2xl p-8">
                    <h3 class="text-lg font-bold text-gray-800 mb-4">Foaia de Parcurs (Pașii de Urmat)</h3>
                    <div class="space-y-6">
                        <div class="flex gap-4 relative">
                            <div class="absolute left-[11px] top-2 bottom-0 w-0.5 bg-gray-200"></div>
                            <div class="w-6 h-6 rounded-full bg-gray-900 text-white flex items-center justify-center flex-shrink-0 text-xs font-bold z-10">1</div>
                            <div>
                                <h4 class="font-bold text-gray-900 text-sm">Pregătirea Documentelor</h4>
                                <p class="text-xs text-gray-500 mt-1">Obține semnătura electronică și cere oferte de la furnizori.</p>
                            </div>
                        </div>
                        <div class="flex gap-4 relative">
                            <div class="absolute left-[11px] top-2 bottom-0 w-0.5 bg-gray-200"></div>
                            <div class="w-6 h-6 rounded-full bg-gray-900 text-white flex items-center justify-center flex-shrink-0 text-xs font-bold z-10">2</div>
                            <div>
                                <h4 class="font-bold text-gray-900 text-sm">Depunerea Cererii</h4>
                                <p class="text-xs text-gray-500 mt-1">Încarcă proiectul pe platforma oficială (MySMIS/IMM Recover) înainte de termen.</p>
                            </div>
                        </div>
                        <div class="flex gap-4 relative">
                            <div class="w-6 h-6 rounded-full bg-green-500 text-white flex items-center justify-center flex-shrink-0 text-xs font-bold z-10">3</div>
                            <div>
                                <h4 class="font-bold text-gray-900 text-sm">Implementarea</h4>
                                <p class="text-xs text-gray-500 mt-1">După aprobare, faci achizițiile și depui cererea de rambursare.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

    </main>

    <script is:inline define:vars={{ cofinantareProcent, valoareMaxGrant }}>
        const input = document.getElementById('investitieInput');
        const grantDisplay = document.getElementById('grantDisplay');
        const cofinantareDisplay = document.getElementById('cofinantareDisplay');

        const formatNumber = (num) => Math.round(num).toLocaleString('ro-RO');

        const calculate = () => {
            const investitie = parseFloat(input.value) || 0;
            
            let grantCalculat = investitie * (1 - cofinantareProcent);
            if (grantCalculat > valoareMaxGrant) {
                grantCalculat = valoareMaxGrant;
            }

            const cofinantareCalculata = investitie - grantCalculat;

            grantDisplay.innerText = formatNumber(grantCalculat);
            cofinantareDisplay.innerText = formatNumber(cofinantareCalculata);
        };

        input.addEventListener('input', calculate);
        calculate();
    </script>
</Layout>